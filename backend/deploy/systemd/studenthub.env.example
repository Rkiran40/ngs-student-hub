# Example environment file for StudentHub
# Copy to /etc/studenthub.env and update values. Keep this file readable only by root (chmod 600).
# Example:
# sudo cp backend/deploy/systemd/studenthub.env.example /etc/studenthub.env
# sudo chown root:root /etc/studenthub.env && sudo chmod 600 /etc/studenthub.env

# App / Flask
FLASK_ENV=production
SECRET_KEY=change-me-secure
JWT_SECRET_KEY=change-me-secure

# Database
DATABASE_URL=mysql+pymysql://user:password@db-host:3306/studenthub_db?charset=utf8mb4

# Uploads
UPLOAD_FOLDER=/var/lib/studenthub/uploads
MAIL_OUTPUT_DIR=/var/lib/studenthub/sent_emails

# SMTP (preferred) - or use MAIL_* names which are accepted as fallback
# If using Gmail, create an App Password and set SMTP_USER to your email and SMTP_PASSWORD to the app password.
# For production, consider using a transactional provider (SendGrid/Mailgun/SES) for better deliverability.
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
SMTP_USER=your@example.com
SMTP_PASSWORD=super-secret
SMTP_USE_TLS=true
# Optional explicit from address used for outgoing messages
SMTP_FROM=noreply@yourdomain.com

# Optional / recommended
SENTRY_DSN=
FRONTEND_URL=https://your-frontend.example.com

# Security note:
# - Do NOT commit real secrets into Git
# - Restrict permissions: `sudo chmod 600 /etc/studenthub.env`
# - Consider using a secrets manager for production
